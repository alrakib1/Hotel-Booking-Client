import PropTypes from 'prop-types';

const Card = ({ room }) => {
    console.log(room);

    return (
        <div>
          
        </div>
    );
};

Card.propTypes = {
    room: PropTypes.object,
};

export default Card;
